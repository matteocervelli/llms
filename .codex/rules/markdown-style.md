# Markdown Style

Never use `---` horizontal rule dividers in markdown files.

Use heading hierarchy (`##`, `###`) to separate sections instead.
Blank lines between sections are sufficient — visual dividers add noise without meaning.

The only exception is front matter delimiters (YAML `---` at the very top of a file).
